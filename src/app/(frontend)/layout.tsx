import './globals.css'

/**
 * The <html> and <body> tags live in the [locale] layout because lang and dir
 * depend on the active locale.
 */
export default function FrontendLayout({ children }: { children: React.ReactNode }) {
  return children
}
